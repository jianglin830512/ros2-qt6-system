import QtQuick
import QtQuick.Controls
import qt_node
import qt.theme 1.0

Item {
    Label {
        text: "试验数据页面"
        font: Theme.titleFont
        color: Theme.textColor
        anchors.centerIn: parent
    }
}
